# Unworked Gold — secrets template.
# Copy this file to a new file called ".env" (exact name, with the dot)
# in the same folder, and fill in the real values.
# NEVER commit the real .env file. .gitignore keeps it out.

DB_HOST=db.condhfwpzlxrzuadgopc.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASS=
